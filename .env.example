# Meta/Facebook Configuration - single token
# Preferred name
FACEBOOK_ACCESS_TOKEN=your_facebook_token_here_with_access_to_all_accounts

# Optional: explicit accounts if /me/adaccounts is rate-limited
# Comma-separated list: act_123,act_456
# META_ACCOUNT_IDS=


# Google Sheets Configuration
GOOGLE_SHEETS_ID=your_google_sheet_id_here
GOOGLE_SHEETS_CREDENTIALS_JSON=path_to_google_service_account.json

# Configuration par défaut
DEFAULT_LOOKBACK_DAYS=7
API_VERSION=v23.0

# Parsing Configuration (optionnel)
ENABLE_LLM_PARSING=false
OPENAI_API_KEY=your_openai_key_if_using_llm

# Output mirroring (compat only)
# If true, copies data/current/*.json to repo root after refresh
MIRROR_TO_ROOT=false
